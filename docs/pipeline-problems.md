## Problems with pipeline

### The `npm ci` command can only install with an existing package-lock.json ...

That means that you have to run `npm install` locally and then push new *package.json* and *package-locak.json*
